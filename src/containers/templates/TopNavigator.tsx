import React from 'react';
import Template from 'shared/components/template/Template';
import { withTemplate } from 'shared/hoc/withTemplate';

export default withTemplate(Template, { hideFooter: true });
